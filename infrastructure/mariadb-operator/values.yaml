metrics:
  enabled: true

webhook:
  cert:
    certManager:
      enabled: true

  resources:
    requests: { cpu: 50m, memory: 64Mi }
    limits: { memory: 256Mi }

resources:
  requests: { cpu: 100m, memory: 128Mi }
  limits: { memory: 512Mi }
