---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup

metadata:
  name: pelican-panel-db-daily-backup
  namespace: pelican-panel

spec:
  schedule: "0 0 2 * * *"
  backupOwnerReference: self
  cluster:
    name: pelican-panel-db
