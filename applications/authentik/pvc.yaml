---
apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: authentik-data
  namespace: authentik

spec:
  storageClassName: longhorn

  accessModes:
    - ReadWriteMany

  resources:
    requests:
      storage: 2Gi
