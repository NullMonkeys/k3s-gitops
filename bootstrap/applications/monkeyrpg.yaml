---
apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: monkeyrpg
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"

spec:
  project: applications

  source:
    repoURL: "https://github.com/NullMonkeys/k3s-gitops-private.git"
    targetRevision: HEAD
    path: monkeyrpg

  destination:
    server: https://kubernetes.default.svc
    namespace: monkeyrpg

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
