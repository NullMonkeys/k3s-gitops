---
apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: nullmonkeys-arc-runner-set
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "8"

spec:
  project: infrastructure

  source:
    repoURL: "https://github.com/NullMonkeys/k3s-gitops.git"
    targetRevision: HEAD
    path: infrastructure/nullmonkeys-arc-runner-set

  destination:
    server: https://kubernetes.default.svc
    namespace: nullmonkeys-arc-runner-set

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
